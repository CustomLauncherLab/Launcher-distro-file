local screen = create_window()
screen:open()

local warehouse = load_image("warehouse", "warehouse.png")
local warehouse_img = create_image_renderer(warehouse, 0, 0, 1920, 1080)
screen:add_child(warehouse_img)
